{
  "name": "500-new-tabs-build",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite build --sourcemap",
    "build": "vite build",
    "test": "vitest --globals run",
    "testmon": "vitest --globals",
    "coverage": "vitest run --coverage",
    "build_ff": "vite build && web-ext --source-dir=dist build",
    "test_ff": "vite build && web-ext --source-dir=dist run",
    "sign_ff": "dotenv -- web-ext sign --source-dir=dist --channel=unlisted"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.30.1",
    "dotenv": "^16.5.0",
    "dotenv-cli": "^8.0.0",
    "jsdom": "^22.1.0",
    "vite": "^7.3.0",
    "vitest": "^4.0.16",
    "web-ext": "^8.6.0"
  }
}
